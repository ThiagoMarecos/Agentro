# Nexora - Infraestructura

Carpeta reservada para:
- Configuraciones de despliegue (Kubernetes, ECS, etc.)
- Terraform o IaC
- Configuración de CI/CD
- Secrets management

En la fase actual, la infraestructura base se gestiona via docker-compose en la raíz.
